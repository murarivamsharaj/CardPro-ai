package com.cardpro.auth.enums;

/**
 * Defines the roles available in the CardPro AI platform.
 * Used for role-based access control (RBAC) across all services.
 */
public enum Role {
    USER,
    ADMIN
}
